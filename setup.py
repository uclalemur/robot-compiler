from setuptools import setup

setup(
   name='robot-compiler',
   version='0.0.1',
   description='Robotics codesign and fabrication tool',
   author='UCLA LEMUR',
   author_email='',
   packages=['robot-compiler'],
   install_requires=[], 
)